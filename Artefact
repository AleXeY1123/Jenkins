#!groovy
pipeline {
    agent any
    stages {
        stage('===Test===') {
            steps {
                sh 'file.xml'
            }
        }
    }
    post {
        always {
            junit 'file.xml'
        }
    }
}
