#!groovy
pipeline {
    agent any
    stages {
        stage('===Test===') {
            steps {
                sh 'mkdir -p build/reports'
                sh 'touch build/reports/file.xml'
            }
        }
    }
    post {
        always {
            junit 'build/reports/*.xml'
        }
    }
}
