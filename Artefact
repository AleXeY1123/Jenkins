#!groovy
pipeline {
    agent any
    stages {
        stage('===Test===') {
            steps {
                sh 'mkdir reports'
                sh 'touch file.xml'
            }
        }
    }
    post {
        always {
            junit 'build/reports/**/*.xml'
        }
    }
}
